// Per-guest submission limits are disabled for now (feature is being remodeled) —
// new Jams are created with these sentinel values so they impose no restriction.
export const NO_MAX_LINKS = 1_000_000;
export const NO_MIN_INTERVAL = 0;
